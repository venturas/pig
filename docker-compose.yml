version: "3"
services:
  pig:
    build: .
    ports:
      - "8080:8080"
    environment:
      - FERNET_KEY=${FERNET_KEY}
      - SECRET_KEY=${SECRET_KEY}
    volumes:
      - pig-data:/app/data

volumes:
  pig-data:
